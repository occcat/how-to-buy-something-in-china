interface:
  display_name: "环境检查"
  short_description: "检查 macOS、ego lite、登录态与 LaTeX 导出能力"
  default_prompt: "Use $env-check to verify shopping-research prerequisites and select the PDF export toolchain."
